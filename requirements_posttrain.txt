# Post-training extras (safe on ROCm)
#
# IMPORTANT (ROCm/AMD):
# - Install ROCm PyTorch first (per requirements.txt).
# - Then install lerobot WITHOUT deps to avoid pip swapping your torch build.
#
# Recommended:
#   pip install -r requirements.txt
#   pip install --no-deps -r requirements_posttrain.txt
#
# NOTE: requirements files do NOT support pip CLI flags like --no-deps inline.
lerobot==0.3.3
scipy
wandb

